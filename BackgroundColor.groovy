// @ExecutionModes({ON_SELECTED_NODE})
node.style.setBackgroundColorCode(node.style.getEdge().getColorCode());

// node.style.backgroundColorCode = node.style.getEdge().getColorCode();
// node.style.setBackgroundColor(node.style.getEdge().getColor());

